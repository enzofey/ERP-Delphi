unit Usuario;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.ExtCtrls, Vcl.StdCtrls, Vcl.ComCtrls,
  Vcl.Buttons,
  UsuarioDataModule, ConsultarUsuarioForm,
  ConsultarEntidadeForm, EntidadeDataModule,
  LogDataModule;

type
  TCadUsuario = class(TForm)
    Panel1: TPanel;
    lblCodigo: TLabel;
    lblUsuario: TLabel;
    lblAtivo: TLabel;
    CBAtivo: TCheckBox;
    btnIncluir: TButton;
    btnGravarIncluir: TButton;
    btnGravarAlterar: TButton;
    btnAlterar: TButton;
    btnDesistir: TButton;
    btnConsultar: TButton;
    btnExcluir: TButton;
    btnFechar: TButton;
    edtCodigo: TEdit;
    EdtUsuario: TEdit;
    SBEntidade: TSpeedButton;
    procedure btnFecharClick(Sender: TObject);
    procedure SBEntidadeClick(Sender: TObject);
    procedure btnIncluirClick(Sender: TObject);
    procedure btnDesistirClick(Sender: TObject);
    procedure btnGravarIncluirClick(Sender: TObject);
    procedure btnConsultarClick(Sender: TObject);
    procedure btnAlterarClick(Sender: TObject);
    procedure btnGravarAlterarClick(Sender: TObject);
    procedure btnExcluirClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  CadUsuario: TCadUsuario;

implementation

{$R *.dfm}

Uses MenuPrincipal;

procedure TCadUsuario.btnIncluirClick(Sender: TObject);
begin
 EdtUsuario.Enabled := True;
 CBAtivo.Enabled := True;

 EdtCodigo.Clear;
 EdtUsuario.Clear;

 SBEntidade.Enabled := True;
 btnGravarIncluir.Visible := True;
 btnDesistir.Visible := True;

 btnAlterar.Visible := False;
 btnConsultar.Visible := False;
 btnExcluir.Visible := False;
end;

procedure TCadUsuario.btnGravarIncluirClick(Sender: TObject);
var codigo, usuario, ativo, tela, senha, senha_alterada: string;
    data: tdatetime;
begin
 codigo := EdtCodigo.text;
 usuario := EdtUsuario.text;
 senha := '123456';
 senha_alterada := 'N';
 if CBAtivo.Checked then ativo := 'S'
 else ativo := 'N';
 tela := 'CadUsuario';
 data := now;

 CadUsuarioDataModule.InserirQuery.SQL.Clear;
 CadUsuarioDataModule.InserirQuery.SQL.Text :=
 'insert into usuario (codigo, ativo, usuario, senha, senha_alterada) ' +
 'values ' +
 '(:codigo, :ativo, :usuario, "123456", "N")';
 CadUsuarioDataModule.InserirQuery.ParamByName('codigo').AsString := codigo;
 CadUsuarioDataModule.InserirQuery.ParamByName('usuario').AsString := usuario;
 CadUsuarioDataModule.InserirQuery.ParamByName('ativo').AsString := ativo;

 LogModule.InserirLog.SQL.Clear;
 LogModule.InserirLog.SQL.Text :=
 'insert into logs (descricao, tela, data) values (:descricao, :tela, :data)';
 LogModule.InserirLog.ParamByName('descricao').AsString :=
 'Inseriu o usuário ' + usuario + ' no código ' + codigo + ' e ativo ' + ativo;
 LogModule.InserirLog.ParamByName('tela').AsString := tela;
 LogModule.InserirLog.ParamByName('data').AsDatetime := Data;
 try
 LogModule.InserirLog.ExecSQL;
 CadUsuarioDataModule.InserirQuery.ExecSQL;
 showMessage('Inserido com sucesso!');
 EdtUsuario.Enabled := False;
 CBAtivo.Enabled := False;

 SBEntidade.Enabled := False;
 btnGravarIncluir.Visible := False;
 btnDesistir.Visible := False;

 btnAlterar.Visible := True;
 btnConsultar.Visible := True;
 btnExcluir.Visible := True;
 except
 ShowMessage('Erro na inclusão!');
 end;
end;

procedure TCadUsuario.btnAlterarClick(Sender: TObject);
begin
 if EdtCodigo.Text = '' then begin
 ShowMessage('Usuário não selecionado!');
 end
 else begin
 btnIncluir.Visible := False;
 btnExcluir.Visible := False;
 btnConsultar.Visible := False;

 btnGravarAlterar.Visible := True;
 btnDesistir.Visible := True;

 EdtUsuario.Enabled := True;
 CBAtivo.Enabled := True;
end;
end;

procedure TCadUsuario.btnGravarAlterarClick(Sender: TObject);
var codigo, usuario, ativo, tela: string;
    data: tdatetime;
begin
 codigo := EdtCodigo.text;
 usuario := EdtUsuario.text;
 if CBAtivo.Checked then ativo := 'S'
 else ativo := 'N';
 tela := 'CadUsuario';
 data := now;

 CadUsuarioDataModule.InserirQuery.SQL.Clear;
 CadUsuarioDataModule.InserirQuery.SQL.Text :=
 'update usuario set usuario = :usuario, ativo = :ativo where codigo = :codigo';
 CadUsuarioDataModule.InserirQuery.ParamByName('codigo').AsString := codigo;
 CadUsuarioDataModule.InserirQuery.ParamByName('usuario').AsString := usuario;
 CadUsuarioDataModule.InserirQuery.ParamByName('ativo').AsString := ativo;

 LogModule.InserirLog.SQL.Clear;
 LogModule.InserirLog.SQL.Text :=
 'insert into logs (descricao, tela, data) values (:descricao, :tela, :data)';
 LogModule.InserirLog.ParamByName('descricao').AsString :=
 'Alterou o usuário ' + usuario + ' no código ' + codigo + ' e ativo ' + ativo;
 LogModule.InserirLog.ParamByName('tela').AsString := tela;
 LogModule.InserirLog.ParamByName('data').AsDatetime := Data;
 try
 LogModule.InserirLog.ExecSQL;
 CadUsuarioDataModule.InserirQuery.ExecSQL;
 showMessage('Inserido com sucesso!');
 EdtUsuario.Enabled := False;
 CBAtivo.Enabled := False;

 btnGravarAlterar.Visible := False;
 btnDesistir.Visible := False;

 btnIncluir.Visible := True;
 btnAlterar.Visible := True;
 btnConsultar.Visible := True;
 btnExcluir.Visible := True;
 except
 ShowMessage('Erro na alteração!');
 end;
end;

procedure TCadUsuario.btnConsultarClick(Sender: TObject);
var
  codigo, usuario, ativo: string;
begin
  CadUsuarioDataModule.FDQuery1.Close;
  CadUsuarioDataModule.FDQuery1.Open;
  Application.CreateForm(TConsultarUsuario, ConsultarUsuario);
  codigo := ConsultarUsuario.SelecionarUsuario;
  if codigo <> '' then
  begin
    EdtCodigo.Text := codigo;
    usuario := ConsultarUsuario.Usuario;
    EdtUsuario.Text := usuario;
    ativo := ConsultarUsuario.Ativo;
    CBAtivo.Checked := ativo = 'S';
  end;
end;

procedure TCadUsuario.btnDesistirClick(Sender: TObject);
begin
 EdtUsuario.Enabled := False;
 CBAtivo.Enabled := False;

 SBEntidade.Enabled := False;
 btnGravarIncluir.Visible := False;
 btnDesistir.Visible := False;

 btnAlterar.Visible := True;
 btnConsultar.Visible := True;
 btnExcluir.Visible := True;
end;

procedure TCadUsuario.btnExcluirClick(Sender: TObject);
var codigo, usuario, ativo, tela, senha, senha_alterada: string;
    data: tdatetime;
begin
 codigo := EdtCodigo.text;
 usuario := EdtUsuario.text;
 senha := '123456';
 senha_alterada := 'N';
 if CBAtivo.Checked then ativo := 'S'
 else ativo := 'N';
 tela := 'CadUsuario';
 data := now;

 if EdtCodigo.Text = '' then begin
 ShowMessage('Usuário não selecionado!');
 end
 else begin

 CadUsuarioDataModule.InserirQuery.SQL.Clear;
 CadUsuarioDataModule.InserirQuery.SQL.Text :=
 'delete from usuario where codigo = :codigo';
 CadUsuarioDataModule.InserirQuery.ParamByName('codigo').AsString := codigo;

 LogModule.InserirLog.SQL.Clear;
 LogModule.InserirLog.SQL.Text :=
 'insert into logs (descricao, tela, data) values (:descricao, :tela, :data)';
 LogModule.InserirLog.ParamByName('descricao').AsString :=
 'Excluiu o usuário ' + usuario + ' no código ' + codigo + ' e ativo ' + ativo;
 LogModule.InserirLog.ParamByName('tela').AsString := tela;
 LogModule.InserirLog.ParamByName('data').AsDatetime := Data;
 try
 LogModule.InserirLog.ExecSQL;
 CadUsuarioDataModule.InserirQuery.ExecSQL;
 showMessage('Excluído com sucesso!');
 EdtUsuario.Clear;
 EdtCodigo.Clear;
 except
 ShowMessage('Erro na exclusão!');
 end;
end;
end;

procedure TCadUsuario.SBEntidadeClick(Sender: TObject);
var
  codigo, usuario: string;
begin
 CadEntidadeDataModule.FDQuery1.Close;
 CadEntidadeDataModule.FDQuery1.Open;
 if not Assigned(ConsultarEntidade) then
 Application.CreateForm(TConsultarEntidade, ConsultarEntidade);

 codigo := ConsultarEntidade.SelecionarEntidade;
 usuario := ConsultarEntidade.nome;

 EdtCodigo.Text := codigo;
 EdtUsuario.Text := StringReplace(UpperCase(ConsultarEntidade.nome), ' ', '.', [rfReplaceAll]);
end;

procedure TCadUsuario.btnFecharClick(Sender: TObject);
begin
 (Parent as TTabSheet).PageControl := nil;
 Parent.Free;
end;
end.
